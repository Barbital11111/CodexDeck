# CodexDeck v2.0.5

1. 整理 CodexDeck 主线：账号、API、混合模式、额度显示、通知数据源和增强启动稳定化。
2. 修复混合模式文本请求误携带 `image_generation` 导致部分不支持生图上游返回 403 的问题。
3. 修复 API 导入页余额显示：新增“额度来源”，并在创建 API 条目时正确保存平台账号、密码和订阅模式。
4. 修复 OAuth 授权链接生成：默认回调端口被系统拒绝或保留时，自动回退到本地空闲端口。
